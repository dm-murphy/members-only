<h1><%= @story.title %></h1>

<p><%= @story.body %></p>

<% if user_signed_in? %>
  <li>
    <%= @story.user.name %>
  </li>
  <li>
    <%= @story.user.username %>
  </li>
  <% else %>
    <p>Log in to see who wrote this story</p>
  <% end %>